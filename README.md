# Analyzing-Titanic-Tragedy
Titanic Survival Prediction 📊. Binary classification project analyzing passenger data (Age, Class, Fare, etc.) to model and predict survival status.

To see the rendered output now, run:
make serve
"$BROWSER" "http://localhost:8000/Titanic_Exercise-Solved.html"

This will:

Start a local web server on port 8000
Open the HTML file in your host's default browser
If the server is already running, just open the URL:
"$BROWSER" "http://localhost:8000/Titanic_Exercise-Solved.html"

To stop the server later, run:
pkill -f "python3 -m http.server"
